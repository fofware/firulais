import { ProveedorProductoFilterPipe } from './proveedor-producto-filter.pipe';

describe('ProveedorProductoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProveedorProductoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
