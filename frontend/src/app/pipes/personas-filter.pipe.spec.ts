import { PersonasFilterPipe } from './personas-filter.pipe';

describe('PersonasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
